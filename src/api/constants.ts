/**
 * IF want to change, this property should be in Auth0
 */
export const ALLOWED_LOGOUT_REDIRECTS = ["/", "/app_api/playground/authenticationAPI"] as const;
export const BASE_API_URL = "/app_api";
