"use client";
import "client-only";

/**
 * Placeholder function for to the be implemented client side error handler.
 * @param {Error} err - The error object.
 */
export function errorHandler_C(err: Error) {
    console.log(err);
}
